name: taxi_transforms
version: '1.0.0'
config-version: 2

profile: taxi_transforms

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  taxi_transforms:
    staging:
      +materialized: view
    marts:
      +materialized: table